Install from Hackage/Stackage:

    $ stack buildl persistent-migration

Install from unpacked release tarball or source repo:

    $ cd persistent-migration
    $ stack build

Just play with it without installing:

    $ stack repl
